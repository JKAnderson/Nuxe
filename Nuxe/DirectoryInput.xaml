<UserControl x:Class="Nuxe.DirectoryInput"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Nuxe"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel DataContext="{Binding RelativeSource={RelativeSource AncestorType=local:DirectoryInput}}">
        <Label Content="{Binding Label}"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding Path}"/>
            <Button Grid.Column="1" Click="ButtonBrowse_Click">Browse</Button>
            <Button Grid.Column="2" Click="ButtonExplore_Click">Explore</Button>
        </Grid>
    </StackPanel>
</UserControl>
